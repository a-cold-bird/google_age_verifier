# Age Verification API 环境配置示例
# 复制为 .env 后填写真实值

# 公网客户端令牌签名密钥（必须固定，重启后不失效）
CLIENT_TOKEN_SECRET=replace_with_a_strong_random_hex

# 客户端令牌有效期（秒）
CLIENT_TOKEN_TTL_SECONDS=900

# 限流窗口与阈值（每个来源 IP）
RATE_LIMIT_WINDOW_SECONDS=60
RATE_LIMIT_MAX_REQUESTS=20

# 代理配置（直连部署请保持 false）
USE_PROXY_HEADERS=false
TRUSTED_PROXY_IPS=
ALLOW_LOCAL_BYPASS=true

# 可选：跨域来源白名单（逗号分隔）；留空表示禁用跨域
# 示例：https://age.tokawa-sakiko.xyz,https://www.example.com
CORS_ALLOWED_ORIGINS=

# 可选：Cloudflare Turnstile
TURNSTILE_SECRET_KEY=

# 可选：管理员 API Key（前端通常不使用）
API_KEY=
